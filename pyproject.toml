[project]
name = "image-denoising"
version = "0.1.0"
description = "Project 19 of Deep Learning"
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "pillow",
    "torch",
    "torchvision",
]

[tool.setuptools]
packages = ["src"]